<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - 메멘토</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 50%, #fecfef 100%);
            min-height: 100vh;
        }
        .alert-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.2);
            padding: 40px;
            margin: 50px auto;
            max-width: 700px;
            border: 3px solid #dc3545;
        }
        .alert-header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #f0f0f0;
        }
        .alert-icon {
            color: #dc3545;
            font-size: 4rem;
            margin-bottom: 20px;
        }
        .countdown {
            background: linear-gradient(45deg, #dc3545 0%, #fd7e14 100%);
            color: white;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            margin: 25px 0;
        }
        .countdown-number {
            font-size: 2.5rem;
            font-weight: bold;
            margin-bottom: 5px;
        }
        .info-section {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        .cancel-btn {
            background: linear-gradient(45deg, #28a745 0%, #20c997 100%);
            border: none;
            color: white;
            padding: 15px 30px;
            border-radius: 8px;
            font-size: 1.1em;
            font-weight: bold;
            width: 100%;
            margin-top: 20px;
        }
        .cancel-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            color: white;
        }
        .warning-section {
            background: #fff3cd;
            border-left: 4px solid #ffc107;
            padding: 20px;
            margin: 20px 0;
        }
        .blinking {
            animation: blink 1s infinite;
        }
        @keyframes blink {
            0%, 50% { opacity: 1; }
            51%, 100% { opacity: 0.3; }
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark" style="background: linear-gradient(45deg, #667eea 0%, #764ba2 100%);">
        <div class="container">
            <a class="navbar-brand" href="#">
                <i class="bi bi-heart-pulse"></i> 메멘토
            </a>
            <span class="navbar-text text-danger fw-bold blinking">
                <i class="bi bi-exclamation-triangle"></i> 긴급 알림
            </span>
        </div>
    </nav>

    <div class="container">
        <div class="alert-card">
            <div class="alert-header">
                <i class="bi bi-exclamation-triangle-fill alert-icon blinking"></i>
                <h1 class="text-danger mb-3">사망 신고 접수 알림</h1>
                <h4 class="text-muted">귀하의 사망이 신고되었습니다</h4>
            </div>

            <div class="countdown">
                <div class="countdown-number" id="countdown"><%= hoursRemaining %></div>
                <div>시간 후 유언 집행 시작</div>
                <div class="small mt-2">
                    집행 예정 시간: <%= new Date(report.executionStartTime).toLocaleString('ko-KR') %>
                </div>
            </div>

            <div class="info-section">
                <h5><i class="bi bi-info-circle"></i> 신고 정보</h5>
                <div class="row">
                    <div class="col-md-6">
                        <p><strong>신고자:</strong> <%= report.reporterName %></p>
                        <p><strong>신고자 이메일:</strong> <%= report.reporterEmail %></p>
                    </div>
                    <div class="col-md-6">
                        <p><strong>신고 일시:</strong> <%= new Date(report.createdAt).toLocaleString('ko-KR') %></p>
                        <p><strong>확인 완료:</strong> <%= new Date(report.confirmedAt).toLocaleString('ko-KR') %></p>
                    </div>
                </div>
                
                <% if (report.additionalInfo) { %>
                <div class="mt-3">
                    <h6>추가 정보:</h6>
                    <p class="text-muted"><%= report.additionalInfo %></p>
                </div>
                <% } %>
            </div>

            <div class="warning-section">
                <h5><i class="bi bi-exclamation-triangle"></i> 중요 안내</h5>
                <ul class="mb-0">
                    <li><strong>이것이 오탐지라면</strong> 아래 "오탐지입니다" 버튼을 즉시 클릭하세요.</li>
                    <li><strong><%= hoursRemaining %>시간 후</strong> 자동으로 디지털 유언이 집행됩니다.</li>
                    <li><strong>집행되면 취소할 수 없습니다.</strong> 신중하게 판단하세요.</li>
                    <li>유언 집행 시 디지털 자산의 삭제, 이관, 추모 공간 생성 등이 진행됩니다.</li>
                </ul>
            </div>

            <div class="text-center">
                <h4 class="text-danger mb-3">
                    <i class="bi bi-question-circle"></i> 이것이 오탐지입니까?
                </h4>
                
                <p class="text-muted mb-4">
                    만약 귀하가 살아계시고 이 신고가 잘못되었다면, 
                    <strong>반드시</strong> 아래 버튼을 클릭하여 신고를 취소하세요.
                </p>
                
                <button type="button" class="cancel-btn" onclick="cancelDeathReport()">
                    <i class="bi bi-shield-check"></i> 네, 오탐지입니다. 신고를 취소합니다
                </button>
                
                <p class="text-muted mt-3 small">
                    버튼 클릭 시 즉시 모든 처리가 중단됩니다.
                </p>
            </div>

            <div class="mt-4 pt-4 border-top text-center">
                <h6>신뢰 연락처 확인 현황</h6>
                <% report.confirmations.forEach(function(confirmation) { %>
                    <div class="badge bg-<%= confirmation.confirmed ? 'success' : 'danger' %> me-2 mb-2">
                        <%= confirmation.contactName %>: 
                        <%= confirmation.confirmed ? '확인' : '거부' %>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Real-time countdown
        function updateCountdown() {
            const executionTime = new Date('<%= report.executionStartTime %>').getTime();
            const now = new Date().getTime();
            const timeRemaining = executionTime - now;
            
            if (timeRemaining <= 0) {
                document.getElementById('countdown').innerHTML = '0';
                document.querySelector('.countdown div:nth-child(2)').innerHTML = '유언 집행 시작됨';
                return;
            }
            
            const hours = Math.ceil(timeRemaining / (1000 * 60 * 60));
            document.getElementById('countdown').innerHTML = hours;
        }
        
        // Update countdown every minute
        setInterval(updateCountdown, 60000);
        
        // Cancel death report
        async function cancelDeathReport() {
            const confirmMessage = `정말로 사망 신고를 취소하시겠습니까?

이 작업은:
- 모든 유언 집행을 중단합니다
- 신뢰 연락처들에게 취소 알림을 보냅니다
- 즉시 정상 서비스로 복귀합니다

계속하시겠습니까?`;
            
            if (!confirm(confirmMessage)) {
                return;
            }
            
            try {
                const button = document.querySelector('.cancel-btn');
                button.disabled = true;
                button.innerHTML = '<i class="bi bi-hourglass-split"></i> 취소 처리 중...';
                
                const response = await fetch('/death-notification/cancel', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });
                
                const result = await response.json();
                
                if (result.success) {
                    alert('사망 신고가 성공적으로 취소되었습니다.\n\n대시보드로 이동합니다.');
                    window.location.href = '/dashboard';
                } else {
                    alert('오류가 발생했습니다: ' + result.message);
                    button.disabled = false;
                    button.innerHTML = '<i class="bi bi-shield-check"></i> 네, 오탐지입니다. 신고를 취소합니다';
                }
            } catch (error) {
                alert('네트워크 오류가 발생했습니다: ' + error.message);
                button.disabled = false;
                button.innerHTML = '<i class="bi bi-shield-check"></i> 네, 오탐지입니다. 신고를 취소합니다';
            }
        }
        
        // Prevent user from navigating away without cancelling
        window.addEventListener('beforeunload', function(e) {
            const confirmationMessage = '사망 신고가 진행 중입니다. 페이지를 떠나면 유언이 집행될 수 있습니다.';
            e.returnValue = confirmationMessage;
            return confirmationMessage;
        });
    </script>
</body>
</html>
