<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - 메멘토</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
        }
        .verification-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            padding: 40px;
            margin: 30px auto;
            max-width: 700px;
        }
        .header-section {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 25px;
            border-bottom: 2px solid #f0f0f0;
        }
        .info-section {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        .warning-section {
            background: #fff3cd;
            border-left: 4px solid #ffc107;
            padding: 20px;
            margin: 20px 0;
        }
        .action-buttons {
            display: flex;
            gap: 15px;
            margin-top: 30px;
        }
        .btn-confirm {
            background: linear-gradient(45deg, #28a745 0%, #20c997 100%);
            border: none;
            color: white;
            padding: 15px 30px;
            border-radius: 8px;
            flex: 1;
        }
        .btn-deny {
            background: linear-gradient(45deg, #dc3545 0%, #fd7e14 100%);
            border: none;
            color: white;
            padding: 15px 30px;
            border-radius: 8px;
            flex: 1;
        }
        .contact-info {
            background: #e3f2fd;
            border-left: 4px solid #2196f3;
            padding: 15px;
            margin: 15px 0;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark" style="background: linear-gradient(45deg, #667eea 0%, #764ba2 100%);">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-heart-pulse"></i> 메멘토
            </a>
        </div>
    </nav>

    <div class="container">
        <div class="verification-card">
            <div class="header-section">
                <i class="bi bi-shield-check text-primary" style="font-size: 3.5rem;"></i>
                <h2 class="mt-3 mb-2">사망 확인 요청</h2>
                <p class="text-muted">신뢰 연락처로서 사망 확인이 필요합니다</p>
            </div>

            <div class="contact-info">
                <h6><i class="bi bi-person-badge"></i> 확인 요청 대상</h6>
                <p class="mb-1"><strong>신뢰 연락처:</strong> <%= contact.name %>님 (<%= contact.email %>)</p>
                <p class="mb-0">귀하는 아래 사용자의 신뢰 연락처로 등록되어 있습니다.</p>
            </div>

            <div class="info-section">
                <h5><i class="bi bi-exclamation-circle"></i> 신고 정보</h5>
                <div class="row">
                    <div class="col-md-6">
                        <p><strong>고인:</strong> <%= report.deceasedName %></p>
                        <p><strong>신고자:</strong> <%= report.reporterName %></p>
                    </div>
                    <div class="col-md-6">
                        <p><strong>신고자 이메일:</strong> <%= report.reporterEmail %></p>
                        <p><strong>신고 일시:</strong> <%= new Date(report.createdAt).toLocaleString('ko-KR') %></p>
                    </div>
                </div>
                
                <% if (report.additionalInfo) { %>
                <div class="mt-3">
                    <h6>추가 정보:</h6>
                    <p class="text-muted"><%= report.additionalInfo %></p>
                </div>
                <% } %>

                <% if (report.certificateFile) { %>
                <div class="mt-3">
                    <p><i class="bi bi-file-earmark-pdf"></i> 사망확인서가 첨부되었습니다.</p>
                </div>
                <% } %>
            </div>

            <div class="warning-section">
                <h6><i class="bi bi-exclamation-triangle"></i> 중요 안내</h6>
                <ul class="mb-0">
                    <li><strong>신중한 판단이 필요합니다.</strong> 이 확인이 완료되면 72시간 후 고인의 디지털 유언이 자동 집행됩니다.</li>
                    <li><strong>허위 확인은 법적 책임을 질 수 있습니다.</strong></li>
                    <li><strong>확실하지 않다면</strong> '확인 거부'를 선택해 주세요.</li>
                    <li><strong>신뢰 연락처 2명 모두 확인해야</strong> 처리가 진행됩니다.</li>
                </ul>
            </div>

            <div class="text-center">
                <h5 class="mb-3">위 정보가 정확한 사망 신고입니까?</h5>
                <div class="action-buttons">
                    <button type="button" class="btn btn-deny" onclick="confirmAction(false)">
                        <i class="bi bi-x-circle"></i> 확인 거부
                    </button>
                    <button type="button" class="btn btn-confirm" onclick="confirmAction(true)">
                        <i class="bi bi-check-circle"></i> 사망 확인
                    </button>
                </div>
                
                <p class="text-muted mt-3 small">
                    선택하신 내용은 취소할 수 없습니다. 신중하게 결정해 주세요.
                </p>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        async function confirmAction(confirmed) {
            const action = confirmed ? '사망을 확인' : '확인을 거부';
            const confirmMessage = `정말로 ${action}하시겠습니까?\n\n이 결정은 취소할 수 없습니다.`;
            
            if (!confirm(confirmMessage)) {
                return;
            }
            
            try {
                // Disable buttons to prevent multiple clicks
                const buttons = document.querySelectorAll('.btn-confirm, .btn-deny');
                buttons.forEach(btn => {
                    btn.disabled = true;
                    btn.innerHTML = '<i class="bi bi-hourglass-split"></i> 처리중...';
                });
                
                const response = await fetch(window.location.pathname, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ confirmed: confirmed.toString() })
                });
                
                const result = await response.json();
                
                if (result.success) {
                    alert(result.message);
                    if (result.redirect) {
                        window.location.href = result.redirect;
                    }
                } else {
                    alert('오류가 발생했습니다: ' + result.message);
                    // Re-enable buttons on error
                    buttons.forEach((btn, index) => {
                        btn.disabled = false;
                        if (index === 0) {
                            btn.innerHTML = '<i class="bi bi-x-circle"></i> 확인 거부';
                        } else {
                            btn.innerHTML = '<i class="bi bi-check-circle"></i> 사망 확인';
                        }
                    });
                }
            } catch (error) {
                alert('네트워크 오류가 발생했습니다: ' + error.message);
                // Re-enable buttons on error
                const buttons = document.querySelectorAll('.btn-confirm, .btn-deny');
                buttons.forEach((btn, index) => {
                    btn.disabled = false;
                    if (index === 0) {
                        btn.innerHTML = '<i class="bi bi-x-circle"></i> 확인 거부';
                    } else {
                        btn.innerHTML = '<i class="bi bi-check-circle"></i> 사망 확인';
                    }
                });
            }
        }
    </script>
</body>
</html>
